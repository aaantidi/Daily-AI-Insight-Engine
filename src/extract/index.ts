// =============================================================================
// M2 EXTRACT — 统一导出
// =============================================================================

export { NewsExtractor } from './extractor';
export type { ExtractorOptions } from './extractor';
export { buildExtractionPrompt, buildRemedialPrompt } from './prompt-builder';
